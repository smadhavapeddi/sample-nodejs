spec:
  services:
    - name: sample-nodejs-deploybutton
      git:
        branch: main
        repo_clone_url: https://github.com/smadhavapeddi/sample-nodejs
        run_command: yarn start
    databases:
    - engine: PG
      name: example-db
      num_nodes: 1
      size: db-s-dev-database
      version: "12"
